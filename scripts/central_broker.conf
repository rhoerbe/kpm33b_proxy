# Mosquitto configuration for a local mock of the central broker
# Used for integration testing only.

listener 1884
protocol mqtt
allow_anonymous true

# Logging
log_dest stderr
log_type error
log_type warning
log_type notice
log_type information

# Enable persistence for testing data retention
persistence true
persistence_location /tmp/mosquitto_central/
